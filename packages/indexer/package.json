{
  "name": "@staking-ui/indexer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev --schema ponder_deposits",
    "start": "ponder start --schema ponder_deposits",
    "db": "ponder db",
    "codegen": "ponder codegen",
    "serve": "ponder serve --schema ponder_deposits",
    "lint": "eslint .",
    "typecheck": "tsc",
    "create-schema": "bun run src/create-schema.ts",
    "drop-schema": "bun run src/drop-schema.ts",
    "reset-schema": "bun run src/reset-schema.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "hono": "^4.5.0",
    "pg": "^8.16.3",
    "ponder": "^0.15.12",
    "prettier": "^3.7.3",
    "seismic-viem": "^1.0.52",
    "viem": "^2.21.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.15.6",
    "eslint": "^8.54.0",
    "eslint-config-ponder": "^0.15.12",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.14"
  }
}
